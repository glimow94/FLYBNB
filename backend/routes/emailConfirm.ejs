<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <p>Dear users,</p>
        <b>IL PROPRIETARIO : <%= ownerInfo.name %> <%= ownerInfo.surname %> <%= ownerInfo.email %></b><br/>
        <b>HA ACCETTATO LA PRENOTAZIONE DEL CLIENTE : <%= clientInfo.name %> <%= clientInfo.surname %> <%= clientInfo.email %></b>
        <p>Riepilogo Prenotazione:</p>
        <br/><b>OWNER INFO : </b>
        <ul>
            <li><b>NOME: <%= ownerInfo.title %> <%= ownerInfo.type %></b></li>
            <li><b>INDIRIZZO: <%= ownerInfo.street %> <%= ownerInfo.place %>  <%= ownerInfo.number %></b></li>
            <li><b>PROPRIETARIO: <%= ownerInfo.name %> <%= ownerInfo.surname %> <%= ownerInfo.email %></b></li>
        </ul>
        <br/><b>BOOKING INFO : </b>
        <ul>
            <li><b>STATO: RICHIESTA CONFERMATA</b></li>
            <li><b>CHECK-IN: <%= ownerInfo.checkIn %> CHECK-OUT: <%= ownerInfo.checkOut %></b></li>
            <li><b>NUMERO di OSPITI (Documenti di riconoscimento in Allegato) : <%= length %>  </b>
                <ul>
                    <% guestInfo.forEach(function(guest) { %>
                        <li><b><%= guest.name %> <%= guest.surname%> DATA DI NASCITA: <%= guest.date%></b></li>
                    <% }); %>
                </ul>
            </li>
            <li><b>TASSA DI SOGGIORNO: <%= ownerInfo.cityTax %> €</b></li>
            <li><b>PAGAMENTO EFFETTUATO: <%= ownerInfo.totPrice %> €</b></li>
        </ul>

        <br/><br/>
        Thanks and regards,
        <br/>FlyBnB
    </body>
</html>